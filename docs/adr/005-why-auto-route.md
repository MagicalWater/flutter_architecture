# 暫不實作

> [!WARNING]
> **Historical placeholder — not an Architecture Decision Record.** 本文件只保存第一階段MVP當時的規劃狀態，不代表Template Baseline 1.5.0的current architecture。現行決策索引請讀取`docs/architecture_decisions.md`；目前專案狀態請讀取`docs/project_context.md`。

第一階段 MVP 不展開 ADR。

目前只保留檔案作為未來擴充位置。

請先完成 `Auth + Profile + Protected Route` 主流程。
