name: auth
description: Auth/session 共用 package。
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  api_client:
    path: ../api_client
  core:
    path: ../core
  freezed_annotation: ^2.4.4
  injectable: ^2.5.0
  json_annotation: ^4.9.0
  meta: ^1.12.0
  rxdart: ^0.28.0
  shared_preferences: ^2.3.2
  sqflite: ^2.3.3+1

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  lints: ^4.0.0
  test: ^1.25.0
