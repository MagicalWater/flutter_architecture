name: core
description: Flutter Enterprise Architecture Template 的核心基礎 package。
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.1.0
