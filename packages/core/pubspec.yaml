name: core
description: Flutter Enterprise Architecture Template 的核心基礎 package。
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  meta: ^1.12.0
  rxdart: ^0.28.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
