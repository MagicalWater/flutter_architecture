name: design_system
description: Flutter Enterprise Architecture Template 的 Design System package。
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_screenutil: 5.9.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.1.0
